# Architecture

create-turbo-stack CLI scaffolding React/Vue/Svelte × Express/Fastify starters.

## Boundaries
- **Standalone:** clone and run per README — no Aspen mesh required
- **Grove:** optional products profile demo
- **Kind:** `node-cli`
